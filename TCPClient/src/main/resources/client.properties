# Client network configuration (loaded at runtime).
# Command-line arguments "<serverIp> <port>" override these values, and an
# external client.properties next to the JAR overrides this bundled file.

# Address/hostname of the server to connect to.
server.host=localhost

# TCP port the server listens on.
server.port=3000
